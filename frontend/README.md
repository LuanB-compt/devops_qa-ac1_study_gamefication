# Frontend - Gamification (Vue 3 + Vite)

Frontend para consumir a API do backend Spring Boot.

## Como rodar

Abra um terminal na pasta frontend/ e rode:

npm install
npm run dev

O Vite vai mostrar um endereco tipo `http://localhost:5173` — abra no navegador.
